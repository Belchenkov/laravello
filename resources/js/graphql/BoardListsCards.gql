query BoardListsCards($id: ID!) {
    board(id: $id) {
        id
        color
        title
        lists {
            id
            title
            board_id
            cards {
                id
                title
                order
            }
        }
    }
}
