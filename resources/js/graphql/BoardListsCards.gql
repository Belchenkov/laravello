query BoardListsCards($id: ID!) {
    board(id: $id) {
        color
        title
        lists {
            id
            title
            cards {
                id
                title
                order
            }
        }
    }
}
