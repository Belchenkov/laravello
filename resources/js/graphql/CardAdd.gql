mutation CardAdd(
  $listId: ID!,
  $title: String!,
  $order: Int!
) {
    cardAdd(input: {
      title: $title,
      list_id: $list_id,
      order: $order,
      owner_id: 1
    }) {
    	id
    	title
    	order
  	}
}
